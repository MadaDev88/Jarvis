# UFO Atlas Environment Variables
# No API keys are required for the default map tile configuration.
# The app uses free CARTO basemap tiles and OpenStreetMap data.

# Optional: Custom tile server URL
# NEXT_PUBLIC_TILE_URL=https://your-tile-server.example.com/style.json
